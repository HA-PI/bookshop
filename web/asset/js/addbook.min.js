(function e(t,n,r){function s(o,u){if(!n[o]){if(!t[o]){var a=typeof require=="function"&&require;if(!u&&a)return a(o,!0);if(i)return i(o,!0);var f=new Error("Cannot find module '"+o+"'");throw f.code="MODULE_NOT_FOUND",f}var l=n[o]={exports:{}};t[o][0].call(l.exports,function(e){var n=t[o][1][e];return s(n?n:e)},l,l.exports,e,t,n,r)}return n[o].exports}var i=typeof require=="function"&&require;for(var o=0;o<r.length;o++)s(r[o]);return s})({1:[function(require,module,exports){
'use strict';

(function () {
  var $, alert, alertDiv, confirm, form, jq_form, ref, showAlert;

  $ = require('jquery');

  ref = require('dialog'), alert = ref.alert, confirm = ref.confirm;

  jq_form = require('jquery-form');

  form = jq_form(document)('#form');

  form.ajaxForm({
    beforeSubmit: function beforeSubmit(data, from, options) {
      return form.find('[type=submit]').prop('disabled', true);
    },
    success: function success(arg, statusText, xhr, $form) {
      var code, data;
      data = arg.data, code = arg.code;
      form.find('[type=submit]').prop('disabled', false);
      return showAlert(data, code === 200);
    },
    fail: function fail(responseText, statusText, xhr, $form) {
      return console.log(responseText, statusText, xhr, $form);
    },
    dataType: 'json'
  });

  alertDiv = $('#alert');

  showAlert = function showAlert(text, success) {
    if (success == null) {
      success = true;
    }
    if (success !== true) {
      alertDiv.removeClass('alert-success').addClass('alert-danger');
    } else {
      alertDiv.removeClass('alert-danger').addClass('alert-success');
    }
    alertDiv.find('[role=text]').text(text);
    return alertDiv.fadeIn('normal');
  };

  $('#img').change(function (e) {
    var files, fr, img;
    img = $(this);
    files = $(this).prop('files');
    fr = new FileReader();
    fr.readAsDataURL(files[0]);
    return fr.onload = function (e) {
      return this.result;
    };
  });
}).call(undefined);

},{"dialog":undefined,"jquery":undefined,"jquery-form":undefined}]},{},[1])

//# sourceMappingURL=addbook.min.js.map
